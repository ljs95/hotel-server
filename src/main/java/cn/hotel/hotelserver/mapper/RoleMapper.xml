<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.hotel.hotelserver.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="cn.hotel.hotelserver.model.basic.Role" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="remark" property="remark" jdbcType="VARCHAR" />
    </resultMap>
    <insert id="insert" parameterType="cn.hotel.hotelserver.model.basic.Role">
        insert into Role(name, remark) values(#{name}, #{remark})
    </insert>

    <select id="getRolesByAdminId" resultMap="BaseResultMap">
        SELECT
            r.*
        FROM
            admin a,
            admin_role ar,
            role r
        WHERE
            a.id = ar.aid
            AND ar.rid = r.id
            AND a.id = #{adminId}
    </select>
</mapper>